import { StyleSheet } from "react-native";

export const mainStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#806773',
    alignItems: 'center',
    justifyContent: 'center',
  },
});